apiVersion: v1
kind: Service
metadata:
  name: nexent-mcp
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 5011
      targetPort: 5011
      name: http
    - port: 5015
      targetPort: 5015
      name: http-alt
  selector:
    app: nexent-mcp
