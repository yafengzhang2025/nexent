apiVersion: v1
kind: Service
metadata:
  name: nexent-runtime
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 5014
      targetPort: 5014
      name: http
  selector:
    app: nexent-runtime
